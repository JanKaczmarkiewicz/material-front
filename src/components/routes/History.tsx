import React from "react";
import DataTable from "../Layout/DataTable/DataTable";

interface Props {}

const History: React.FC<Props> = props => {
  return (
    <>
      <DataTable title="Odbyte kolendy" items={[]} config={{}} />
    </>
  );
};

export default History;
