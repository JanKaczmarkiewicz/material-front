import React, { ReactNode } from "react";
import { InputConfig } from "../Table/Inputs/InputFactory";

interface Config {
  label: string;
  input: InputConfig;
}

const createTableContext = (config: Config) => React.createContext(config);

interface Props {
  children: ReactNode;
}

const CreateTableProvider = (context: React.Context<l>) => {
  const TableProvider = ({ children }: Props) => {
    return <context.Provider value={null}>{children}</context.Provider>;
  };
};

export { createTableContext };
